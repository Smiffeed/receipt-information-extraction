[project]
name = "receipt-information-extraction"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Smiffeed", email = "nutchapol.r@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
receipt-information-extraction = "receipt_information_extraction:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
